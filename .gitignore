# 비밀/데이터 — 절대 커밋 금지
.env
.env.*
!.env.example
.streamlit/secrets.toml
*.db
data/*.db
# DB 백업/스냅샷 — 계정 해시 노출 방지(gitignore 우회 방지, 2026-07-21)
*.bak
*.db.bak*
*.db.backup*
*.db-backup*
data/*.db.*
sample_data/*.db.*
data/local_db.json
data/user_state.json
data/_*.png
data/_*.json
data/_*.log

# 캐시/생성물
__pycache__/
*.pyc
*.log
_st.log
downloads/
temp/
cache/
static/thumbnails_raw/
.apify_storage/
playwright/.cache/
.venv/
venv/

# 내부 확인용 샘플은 sample_data/ 에 민감정보 없이만 포함

# ����� clean DB�� ����(����/��� ����)
!sample_data/demo.db
secrets_cloud_paste.toml

# ── 썸네일은 Supabase Storage(series-archive/thumbnails)가 원본 — 레포에는 두지 않는다(2026-08-11) ──
#    로컬 디스크 파일은 그대로 유지되며, 앱은 DB에 저장된 공개 URL로 이미지를 불러온다.
static/thumbnails/

# 배포용 Secrets 원문(서비스 키 포함) — 절대 커밋 금지
_deploy_secrets.toml
